---
# tasks file for roles/osp-instance-delete
- name: Fetch Instance Info
  os_server_facts:
    cloud: ospcloud
    server: "{{ servername }}"
  register: instanceid
  
- name: Delete instances
  os_server:
    name: "{{ instanceid }}"
    state: absent
